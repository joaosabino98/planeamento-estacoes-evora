"""Lógica pura do servidor, organizada por domínio.

Este pacote contém os blocos extraídos de ``server.py`` durante a refatorização.
``server.py`` continua a ser o ponto de entrada (``python3 server.py``) e expõe
a totalidade da superfície pública usada pelos testes — re-exportando os
símbolos definidos aqui.
"""
